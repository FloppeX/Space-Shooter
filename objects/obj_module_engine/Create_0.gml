event_inherited();

thrust = 400;
add_thrust = 0;

// Particles

part_engine_flame = part_type_create();
part_engine_flame = global.part_rocket_smoke

//engine_flame_emitter = part_emitter_create(global.part_system_below);